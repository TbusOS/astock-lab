# 你自己的脱敏替换规则 —— 复制成 personal.rules 再填。
#
# ★ 和 personal.patterns 同一个道理:**规则本身就是敏感的。**
#   「把 <你的用户名> 换成 user」这行字,本身就泄露了 <你的用户名>。
#   所以 personal.rules 被 .gitignore 掉,只有这份带占位的 .example 进仓。
#
# 格式:每行 `正则<TAB>替换<TAB>说明`(中间是**制表符**)。# 开头是注释。
# 个人规则**先于**通用规则执行(它们更具体)。
#
#/home/<你的登录名>/<某个私有仓>	$LAB	私有仓路径
#\b<你的登录名>\b	user	用户名
#<你的私有仓名>	本仓	私有仓名
#<内部项目代号>	某产品	内部代号
