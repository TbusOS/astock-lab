# 你自己的标识清单 —— 复制成 personal.patterns 再填。
#
# ★ 这个文件为什么单独存在:**清单本身就是敏感的。**
#   把「要查 <你的用户名>、<你的私有仓名>、<你司内部代号>」写进公开仓的脚本里,
#   等于给任何人一份「该找什么」的索引 —— 比它防住的单点泄露更糟。
#   所以 personal.patterns 被 .gitignore 掉,只有这份带占位的 .example 进仓。
#
# 格式:每行 `标签<TAB>正则`(中间是**制表符**,不是空格)。# 开头是注释。
# 正则给 grep -E 用。
#
# 下面几行把 <> 里的换成你自己的,然后删掉行首的 #:

#无我的用户名	\b<你的登录名>\b|\b<你的另一台机器的登录名>\b
#无我的主机名	\b<你的主机名>\b|\b<内网机器名>[0-9]*\b
#无我的私有仓名	<私有仓1>|<私有仓2>|<私有仓3>
#无内部项目代号	\b<项目代号1>\b|<项目代号2>|<产品线代号>
#无我司标识	<公司英文简称>|<公司域名>
