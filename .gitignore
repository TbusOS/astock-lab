# 取数落盘与个人数据。
# ⚠ 写 data/* 而不是 data/ 是**故意的**:git 不允许「父目录被排除后再反选子文件」,
#   写成 data/ 会让私有副本永远没法 track 自己的数据。写 data/* 只排除内容不排除
#   目录本身,私有副本加一个 data/.gitignore(内容 !*)就能把数据收进自己的仓,
#   而那个文件只存在于私有侧,从本仓同步时零冲突。
data/*
!data/.gitkeep

# 个人的东西一律走这个目录 —— 本仓不产生它,私有副本自己建。
# ⚠ 同样写 private/* 而不是 private/,理由见上面 data 那段:
#   写成 private/ 的话私有副本永远没法 track 自己的 private/ 内容。
#   (2026-08-28 第一版就写错成 private/,私有副本 6.4M 个人文件一个都收不进去。)
private/*
!private/.gitkeep

# 生成的报告(含持仓成本这类个人信息)
reports/

# 第三方开源仓,各自有独立 .git
repos/

# ★ 你自己的标识清单 —— 用户名 / 主机名 / 私有仓名 / 内部代号。
#   **清单本身就是敏感的**:发布出去等于给别人一份「该找什么」的索引,
#   比它防住的单点泄露更糟。只有带占位的 .example 进仓。
scripts/personal.patterns
scripts/personal.rules

# 环境
# install.sh 生成的别名文件,里面是本机绝对路径
aliases.sh
# ⚠ 不带斜杠:很多人的 .venv 是**指向别处的软链**,而 `.venv/` 只匹配目录,
#   匹配不到软链 —— 那个软链就会被当成普通文件提交上去。
.venv
__pycache__/
*.pyc

# 编辑器 / 系统
.DS_Store
*.swp
shots/
